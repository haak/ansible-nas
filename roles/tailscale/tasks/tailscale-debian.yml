# install tailscale on ubuntu
- name: install tailscale package via apt
  apt:
    name: tailscale
    state: present
    update_cache: yes
  when: ansible_os_family == "Debian"

# enable tailscale service
- name: enable tailscale service
  service:
    name: tailscaled
    enabled: yes
    state: started
  when: ansible_os_family == "Debian"

# run the command tailscale up if tailscale key is defined
- name: run tailscale up
  command: tailscale up --authkey={{ tailscale_key }}
  when: tailscale_key is defined

